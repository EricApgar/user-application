[project]
name = "user-application"
version = "0.1.0"
description = "User run application to control a remote device."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "nicegui>=3.4.1",
]
